#docker build
docker compose up --build -d
docker compose ps
docker compose logs -f

#restart
docker compose down
#rebuild
docker compose up --build -d


docker compose exec graph-app-only env | grep -E 'TENANT_ID|CLIENT_ID|CLIENT_SECRET'
docker compose down
docker compose up --build -d

#Test OAuth client-credntials
docker compose exec graph-app-only sh -lc '
curl -s -X POST "https://login.microsoftonline.com/$TENANT_ID/oauth2/v2.0/token" \
 -H "Content-Type: application/x-www-form-urlencoded" \
 -d "client_id=$CLIENT_ID&client_secret=$CLIENT_SECRET&scope=https%3A%2F%2Fgraph.microsoft.com%2F.default&grant_type=client_credentials"
'
